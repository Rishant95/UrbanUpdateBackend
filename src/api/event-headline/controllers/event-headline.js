'use strict';

/**
 * event-headline controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::event-headline.event-headline');
